initialEnergy = `cat system | grep -v '^#' | cut -f2 2>/dev/null | head -1`

set nokey
set xlabel "time / years"
set ylabel "E_{system} / initial E_{system}"

plot 'system' u 1:($2 / initialEnergy) w l

pause -1